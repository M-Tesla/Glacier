zstd 1.5.7 from facebook/zstd (BSD / GPLv2 dual; Glacier uses BSD).
Headers plus `lib/common`, `lib/compress`, `lib/decompress` are compiled into
libglacier so the wheel does not need system libzstd. Assembly is off
(`ZSTD_DISABLE_ASM`). Pin: `build.zig.zon` `.zstd`.
